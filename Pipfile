[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
django = "*"
gunicorn = "*"

[requires]
python_version = "3.8"

[scripts]
start_prod = "bash -c 'cd shop && gunicorn shop.wsgi'"
install_caddy = "bash scripts/install-caddy.bash"
